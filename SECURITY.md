# 安全策略

## 支持的版本

| 版本 | 是否支持 |
|------|----------|
| 1.0.x | ✅ 支持 |
| < 1.0 | ❌ 不支持 |

## 报告漏洞

**请不要通过公开 issue 报告安全漏洞。**

请前往仓库的 *Security → Report a vulnerability* 提交私密安全公告，或直接联系维护者。
报告时请尽量包含：

- 漏洞类型与影响范围
- 复现步骤（可附最小复现代码或请求）
- 潜在危害评估

我们会在 72 小时内确认，修复后会在本文件与 Release Notes 中致谢报告者（除非你要求匿名）。

## 已知安全设计

本项目面向本地 / 内网部署，已内置以下防护，修改时请勿削弱：

| 机制 | 说明 |
|------|------|
| CSRF | 全部常规 POST 路由由 Flask-WTF 校验 token，仅 3 个纯前端 AJAX 接口豁免 |
| 限流 | Flask-Limiter：全局 50 次/小时，`/collect` 5 次/小时 |
| 密钥管理 | 所有密钥走环境变量 / `.env`，`.env` 已在 `.gitignore` 中，禁止硬编码进代码 |
| 分享 / 提交打包 | `.gitignore` 只挡得住 Git：把项目目录整体拷贝或打 zip 提交前，必须删掉 `.env`（真实密钥随目录分发），只保留 `.env.example`；已分发过的密钥要在对应平台作废重签 |
| 输入校验 | URL 数值参数做正整数校验，越界返回 400；分页链接统一走 `url_for()` |
| Agent 工具调用 | 工具参数由代码预定义后直接调用，不经 LLM 动态传参 |
| 信息脱敏 | 前端异常提示不暴露内部路径与堆栈 |

公网部署前请务必设置固定的 `FLASK_SECRET`，并确认未开启 Debug 模式（根目录不存在 `.debug` 文件）。
